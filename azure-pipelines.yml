# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: 'MyAgentPool'
  demands:
    -npm

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Azure for Students(6e57b072-ba3d-47a7-9e41-bf68eb9503bd)'
    scriptType: 'ps'
    scriptLocation: 'inlineScript'
    inlineScript: |
      cd C:\users\joao\Documents
      git clone https://github.com/Dickmann25/DevOps_test.git
      